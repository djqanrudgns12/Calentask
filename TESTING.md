# 🧪 브라우저 테스트 및 데스크톱 셋업 가이드

본 프로젝트는 Supabase를 백엔드로 사용하며, Playwright를 통한 브라우저 테스트는 **공용 원격 DB** 환경에서 진행됩니다.
이 가이드는 데스크톱(개발 환경)을 변경하거나 새로 세팅할 때 원활한 테스트를 진행하기 위한 A to Z 가이드입니다.

---

## 1. 🚀 데스크톱 셋업 (1분 컷)

깃허브에서 클론을 받은 후, 별도의 DB 시딩(Seeding) 없이도 바로 브라우저 테스트가 가능합니다. 원격 DB에 이미 테스터 계정과 데이터가 준비되어 있기 때문입니다.

1. **저장소 클론 및 패키지 설치**
   ```bash
   git clone <repository_url>
   cd Calentask
   npm install
   ```

2. **환경 변수 세팅**
   - 최상단 경로에 `.env.local` 파일을 생성합니다.
   - `.env.example` 파일의 내용을 복사하여 넣고, 실제 키 값으로 변경합니다.
   - 팀 내 공유된 `SUPABASE_SERVICE_ROLE_KEY`와 `SEED_ALLOWED_PROJECT_REF`도 꼭 포함하세요.

3. **개발 서버 실행**
   ```bash
   npm run dev
   ```

4. **Playwright 테스트 (Agent 또는 수동)**
   - 브라우저 테스트 스크립트가 `http://localhost:3000`을 향하게 한 뒤 테스트를 시작하세요.

---

## 2. 👥 원격 DB 테스터 계정 목록

원격 DB에는 브라우저 테스트를 위한 3개의 고정 계정이 상주하고 있습니다.

| 용도 | 로그인 아이디 | 비밀번호 | 내부 이메일 | 데이터 상태 |
|------|-------------|----------|------------|-------------|
| **부하 테스트용** | `tester1` | `test1234!` | `tester1@calentask.com` | 캘린더, 아카이브 노트 데이터 매우 풍부 |
| **일반 렌더링용** | `tester2` | `test1234!` | `tester2@calentask.com` | 일반적인 사용자와 비슷한 수준의 데이터 |
| **빈 상태(Empty)** | `tester3` | `test1234!` | `tester3@calentask.com` | 데이터 없음 (최초 가입 시 UI 렌더링 확인용) |

> ⚠️ **주의:** 로그인 시 **아이디만** 입력하세요 (예: `tester1`). 이메일 전체(`tester1@calentask.com`)를 입력하면 로그인에 실패합니다. 서버가 아이디에 `@calentask.com`을 자동으로 붙여 인증합니다.

---

## 3. 🧹 테스터 환경 초기화 (Reset)

테스트 도중 삭제/수정 등으로 데이터가 오염되었거나 데이터가 모자란 경우, 명령어 한 줄로 원격 DB의 테스터 환경을 **원상 복구(초기화)**할 수 있습니다.

```bash
npm run seed
```

**동작 방식:**
1. 기존 테스터 계정(`@calentask.com`)의 데이터를 모두 안전하게 지웁니다. (실제 유저 보호)
2. `tester1`, `tester2`, `tester3` 계정을 새로 만듭니다.
3. 무작위 더미 데이터(faker.js 활용)를 대량으로 다시 쏟아붓습니다.

> 🚨 **보안 안전장치:**
> `npm run seed`는 아무 DB에서나 동작하지 않습니다. `.env.local`에 명시된 `SEED_ALLOWED_PROJECT_REF` 값과 현재 연결된 Supabase URL이 일치할 때만 실행되어, 프로덕션 DB를 실수로 날리는 것을 방지합니다.
